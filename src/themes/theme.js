import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#680ff1',
        },
        secondary: {
            main: '#fff',
        },
        greish: {
            main: '#797d76'
        }
    },
});


export default theme;